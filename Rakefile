# frozen_string_literal: true

require "rake"
require "rspec/core/rake_task"

# Define the RSpec task
RSpec::Core::RakeTask.new(:spec)

# Alias `rake` and `rake test` to run specs
task default: :spec
task test: :spec
